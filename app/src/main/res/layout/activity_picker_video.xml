<?xml version="1.0" encoding="utf-8"?>
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    style="@style/LayoutMatch"
    android:id="@+id/picker_video_parent_view"
    android:background="@color/colorLighterGrey"
    android:orientation="vertical">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_gravity="center"
        android:orientation="vertical">

        <include layout="@layout/widget_toolbar" />

        <android.support.v7.widget.RecyclerView
            android:id="@+id/picker_video_files_list_rv"
            style="@style/LayoutMatch"
            android:background="@android:color/transparent"
            android:divider="@android:color/transparent"
            android:listSelector="@android:color/transparent"
            android:scrollbars="none" />

    </LinearLayout>

    <RelativeLayout
        android:id="@+id/picker_video_view_pager_lay"
        style="@style/LayoutMatch"
        android:background="@color/colorBlack"
        android:visibility="gone">

        <android.support.v4.view.ViewPager
            android:id="@+id/picker_video_pager"
            style="@style/LayoutMatch"
            android:layout_above="@+id/bottom_lay" />

        <LinearLayout
            android:id="@+id/bottom_lay"
            style="@style/LayoutMatchWrap"
            android:layout_alignParentBottom="true"
            android:gravity="center"
            android:orientation="horizontal"
            android:padding="@dimen/size10"
            android:weightSum="2">

            <Button
                android:id="@+id/picker_video_cancel_btn"
                style="@style/LayoutMatchWrap"
                android:layout_width="0dip"
                android:layout_height="@dimen/size40"
                android:layout_margin="@dimen/size5"
                android:layout_weight="1"
                android:background="@color/colorOrange"
                android:text="@string/cancel"
                android:textSize="@dimen/text14"
                android:visibility="visible" />

            <Button
                android:id="@+id/picker_video_select_btn"
                style="@style/LayoutMatchWrap"
                android:layout_width="0dip"
                android:layout_height="@dimen/size40"
                android:layout_margin="@dimen/size5"
                android:layout_weight="1"
                android:background="@color/colorOrange"
                android:text="@string/select"
                android:textSize="@dimen/text14"
                android:visibility="visible" />

        </LinearLayout>

    </RelativeLayout>

</FrameLayout>
